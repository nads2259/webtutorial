"""Northstar knowledge module (IMPL-009).

Versioned, addressable, typed content: a canonical tree of typed blocks with stable block ids
(LAW-06/ARCH-006), draft -> review -> publication workflow with IMMUTABLE published revisions and
provenance (LAW-07/ARCH-007), sanitized HTML/Markdown projections (FR-CNT-004), taxonomy and
object-storage-backed media behind a port (FR-CNT-009). Hexagonal: the ``domain`` layer is pure;
infrastructure lives behind ports in ``adapters``.
"""
