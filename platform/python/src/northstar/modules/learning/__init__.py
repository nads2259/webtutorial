"""Northstar learning & assessment module (the reference product core, JRN-001).

Owns the learning design hierarchy (domain -> path -> course -> section/block over PUBLISHED
knowledge revisions), progress stored INDEPENDENTLY from analytics with a stable resume position,
a private personal overlay (bookmark/note/highlight), versioned assessment items with deterministic
scoring, completion & credentials derived from EXPLICIT rules over auditable evidence, explainable
consent/entitlement-aware recommendations, a transparent inspect/correct/reset inferred profile,
and an AI tutor that REUSES the single governed ``ai.answer`` pipeline as a scoped actor
(multilingual + a deterministic pedagogical rubric; never leaks an assessment answer key).

See ``module.yaml`` for the manifest; FR-LRN-001..007.
"""
